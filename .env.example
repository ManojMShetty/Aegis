# Copy to `.env` and fill in. `.env` is gitignored and never enters the eval container.
#
# Google AI Studio (Gemini) - free tier, no credit card: https://aistudio.google.com
GEMINI_API_KEY=your-key-here
#
# Optional: Anthropic, for a Claude quality-comparison run (a paired A/B needs only
# that BOTH arms use the same model - the free Gemini key is enough for the headline).
# ANTHROPIC_API_KEY=your-key-here
